# Ticketing-Tool-Database
MYSQL WorkBench software can be used to run this project. The details about the scope, conceptual design, normalisation, table description, views, events, triggers and stored procedures are explained in the document.
A small client application is also provided which connects to the database instance over AWS and fetches the query results.
